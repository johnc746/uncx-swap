import { styled } from '@mui/system';

export const Image = styled('img')({
    width: '60px',
    height: '60px'
});

export const MidIcon = styled('img')({
    width: '40px',
    height: '40px'
});

export const SmallIcon = styled('img')({
    width: '30px',
    height: '30px'
});